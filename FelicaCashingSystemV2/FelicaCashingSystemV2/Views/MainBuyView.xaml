<UserControl x:Class="FelicaCashingSystemV2.Views.MainBuyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="700">
    <UserControl.Resources>
        <Style TargetType="{x:Type controls:Tile}"
               BasedOn="{StaticResource MainTile}">
            <Setter Property="Command" Value="{Binding BuyCommand}"/>
            <Setter Property="CommandParameter" Value="{Binding Count,RelativeSource={x:Static RelativeSource.Self}}"/>
            <Setter Property="Title" Value="購入"/>
        </Style>
    </UserControl.Resources>
    
    <WrapPanel>
        <controls:Tile Count="30"/>
        <controls:Tile Count="110" />
        <controls:Tile Count="150" />
        <controls:Tile Count="350"/>
        <controls:Tile Count="..." CommandParameter="-1"/>
    </WrapPanel>
</UserControl>
